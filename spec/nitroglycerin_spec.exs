defmodule NitroglycerinSpec do
  use ESpec

  # ARG. Learn to write tests in Elixir…

  # let :source_io, do: File.open!(source_content, [:ram, :binary])

  # describe "#encrypt!" do
  #   subject do
  #     Nitroglycerin.encrypt!(source_io, pad)
  #     File.read!(target_path)
  #   end

  #   let :source_content, do: "content"

  #   it do: is_expected() |> to(have_hd "NG")
  # end
end